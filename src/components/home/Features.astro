<section class="features" id="caracteristicas">
  <div class="features-container">
    <h2 class="features__title">¡Aprende con una nueva experiencia!</h2>
    <div class="features__list">
      <article class="feature">
        <header>
          <div class="feature__preview"></div>
        </header>
        <div class="feature__main">
          <h3 class="feature__title">Interactua con páginas y aprende</h3>
          <p class="feature__description">
            A través de páginas interactivas podrás visualizar el código
            explicado de html, css y javascript de cada una de estas con tan
            solo un <span>¡click!</span>.
          </p>
          <button class="feature__btn">Probar</button>
        </div>
      </article>
      <article class="feature feature--reversed">
        <header>
          <div class="feature__preview"></div>
        </header>
        <div class="feature__main">
          <h3 class="feature__title">Aprende los lenguajes de la Web desde la Web</h3>
          <p class="feature__description">
            Aprende mediante páginas web que incorporan las mejores prácticas, permitiéndote comprender HTML semántico, CSS con enfoque en buenas prácticas y el uso de Javascript optimizado.
          </p>
          <button class="feature__btn">Ver páginas</button>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .features {
    width: 100%;

    &-container {
      width: min(100%, 768px);
      margin: auto;
      display: flex;
      flex-direction: column;
      gap: 1.85rem;
    }
    &__title {
      margin: 0;
      text-align: center;
      font-size: 1.75rem;
    }

    &__list {
      padding: 0 0 3rem 0;
      gap: 3rem;
      display: flex;
      flex-direction: column;
    }
  }

  .feature {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 0;
    gap: 2rem;

    
    &__preview {
      width: 300px;
      height: 300px;
      background-color: black;
      border-radius: 0.25rem;
    }
    &__img {
      border-radius: 0.25rem;
      object-fit: cover;
    }
    &__title {
      margin: 0;
      font-size: 2rem;
      text-wrap: balance;
    }

    &__main {
      display: flex;
      padding: 1.5rem 2rem;
      flex-direction: column;
      gap: 1rem;

      @media (width >= 768px) {
        padding-left: 0;
      }
    }

    &__description {
      margin: 0;
      text-wrap: pretty;
      font-size: 0.925rem;
    }

    &__btn {
      background: #D5B263;
      outline: none;
      line-height: 2rem;
      border: none;
      cursor: pointer;
      font-family: var(--font-family);
      font-weight: 700;
      color: #293040;
      padding: 0 1rem;
      border-radius: 0.25rem;
      width: fit-content;
      transition: transform 0.25s ease;
      &:active {
        transform: scale(0.95);
      }
    }

    @media (width >= 620px) {
      padding: 0 1rem;
    }

    @media (width >= 768px) {
      flex-direction: row;
      &--reversed {
        flex-direction: row-reverse;
      }
    }
  }
</style>
